- Resolved: fix tokenizer dependency loading. Token counting now loads runtime deps from normal installed `node_modules` instead of expecting a non-existent `vendor/node_modules` tree.
- Resolved: fix the npm installation story. The published npm package can now use its installed runtime dependencies directly, so npm installs no longer fall back to approximate counting just because `vendor/` is missing.
- Resolved: fix session/subagent telemetry aggregation. OpenCode stores per-API-call `step-finish` parts, while `assistant.info.tokens` only reflects the latest step on that assistant message. TokenScope now sums the per-call `step-finish` data instead of undercounting multi-step turns.
- Remaining upstream limitation: OpenCode local session/export payloads only persist aggregate `tokens.cache.write` per API call. The Anthropic `cache_write_5m` / `cache_write_1h` split exists in console billing/provider code, but it is not stored in local session messages, so TokenScope cannot recover that breakdown post hoc without upstream storage changes.
- Remaining upstream limitation: OpenCode also does not persist finish-step provider metadata in exported session history. Some providers expose extra response-side numbers (for example accepted/rejected prediction tokens or service-tier metadata), but those values are dropped before session export, so they are not available for after-the-fact TokenScope reporting today.
- Resolved: slim the tokenizer runtime dependency chain. `plugin/package.json:37-40` now uses `@huggingface/tokenizers` instead of `@huggingface/transformers`, and `plugin/tokenscope-lib/tokenizer.ts:6-146` now fetches `tokenizer.json` / `tokenizer_config.json` directly instead of relying on Transformers.js + `onnxruntime-node`. `plugin/install.sh:145-183` was updated to verify the lightweight tokenizer package instead.
- Resolved: avoid auth/error leakage for gated tokenizer hubs. `plugin/tokenscope-lib/config.ts:118-122` no longer uses a gated Google provider default tokenizer, and `plugin/tokenscope-lib/tokenizer.ts:6-160` now skips known non-public Hugging Face hubs before any fetch and emits a generic approximation warning instead of exposing raw 401/auth/network errors in the analysis report.
- Resolved: normalized blank `sessionID` tool arguments in `plugin/tokenscope.ts:56-58,84-104` so OpenCode models that emit `sessionID: ""` now fall back to `context.sessionID` instead of producing the false fallback error "No session ID available for token analysis".
- Resolved: added regression coverage in `plugin/test/tokenscope.test.ts:1-92` for empty-string, whitespace-only, and explicit `sessionID` handling so the current-session fallback cannot silently regress.
- Resolved: hardened `command/tokenscope.md:5-9` so the slash-command prompt now tells the model to leave `sessionID` unset unless the user explicitly asked to analyze a different session.
- Resolved: `plugin/tokenscope-lib/config.ts:10-43` now resolves bundled assets from either the source or compiled `dist/` module layout, so built/npm installs find the published root-level `models.json` and `tokenscope-config.json` instead of silently falling back to default pricing.
- Potential cleanup: `plugin/tokenscope-lib/skill.ts:610-624` wildcard matching has a special-case rewrite for patterns ending in `" .*"`; verify intended permission-pattern semantics before refactoring because this branch is brittle and currently untested.
- Potential cleanup: `plugin/tokenscope-lib/config.ts:75-83` skips bundled config whenever a user config exists; verify whether user config should merge over bundled defaults instead of only hard-coded defaults before changing behavior.
- Resolved: TokenScope now models OpenCode context-size pricing tiers / `context_over_200k` in `plugin/tokenscope-lib/types.ts` and `plugin/tokenscope-lib/cost.ts`, applying tier selection per API call using raw context size (`input + cache.read + cache.write`).
- Resolved: TokenScope now attempts to read OpenCode `session.get` aggregates in `plugin/tokenscope.ts` / `plugin/tokenscope-lib/opencode.ts` and applies persisted session `cost`/`tokens` via `plugin/tokenscope-lib/session-workflow.ts`, while keeping step-finish telemetry for per-model/per-call detail.
